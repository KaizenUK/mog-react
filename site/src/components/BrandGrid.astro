---
const brands = [
  { monogram: "MO", name: "Midland Oil",        href: "/brand/midland-oil" },
  { monogram: "VL", name: "Varol Lubricants",   href: "/brand/varol-lubricants" },
  { monogram: "RO", name: "Rods Oils",           href: "/brand/rods-oils" },
  { monogram: "KL", name: "Kwikfill Ltd",        href: "/brand/kwikfill" },
];
---

<section class="brands">
  <div class="brands__inner">
    <p class="brands__intro">
      Midland Oil Group combines the expertise of 4 in-house brands: Midland Oil, Varol Lubricants, Rods Oils, and Kwikfill Ltd to bring you a comprehensive, all-in-one solution for your lubrication needs. We stock and bulk supply some of the highest performing engine oils and industrial lubricants available on the market.
    </p>

    <h2 class="brands__heading">Lubricant Wholesalers: Our Brands</h2>

    <div class="brands__grid">
      {brands.map((brand) => (
        <a class="brand-card" href={brand.href}>
          <span class="brand-card__monogram">{brand.monogram}</span>
          <span class="brand-card__name">{brand.name}</span>
          <span class="brand-card__cta">View Brand &rarr;</span>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .brands {
    background: var(--page-bg);
    padding: var(--section-py) var(--section-px);
    border-top: 1px solid var(--border-subtle);
  }

  .brands__inner {
    max-width: var(--page-max);
    margin: 0 auto;
  }

  .brands__intro {
    font-family: var(--font-body);
    font-size: clamp(15px, 1.5vw, 17px);
    line-height: 1.75;
    color: var(--text-secondary);
    max-width: 780px;
    margin: 0 0 40px;
  }

  .brands__heading {
    font-family: var(--font-heading);
    font-size: clamp(13px, 1.2vw, 14px);
    font-weight: 400;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--nav-accent);
    margin: 0 0 28px;
  }

  .brands__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
  }

  .brand-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
    padding: 32px 28px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-subtle);
    text-decoration: none;
    transition: border-color 240ms ease, background 240ms ease;
  }

  .brand-card:hover {
    border-color: var(--border-gold);
    background: rgba(200, 160, 52, 0.04);
  }

  .brand-card__monogram {
    font-family: var(--font-heading);
    font-size: 36px;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: var(--nav-accent);
    line-height: 1;
  }

  .brand-card__name {
    font-family: var(--font-heading);
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-primary);
    line-height: 1.2;
  }

  .brand-card__cta {
    font-family: var(--font-heading);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--nav-accent);
    margin-top: auto;
    transition: letter-spacing 200ms ease;
  }

  .brand-card:hover .brand-card__cta {
    letter-spacing: 0.18em;
  }

  @media (max-width: 900px) {
    .brands__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .brands__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
